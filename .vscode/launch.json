{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Wails: Debug Curator",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${workspaceFolder}/build/bin/curator.exe",
            "preLaunchTask": "build",
            "cwd": "${workspaceFolder}",
            "env": {}
        }
    ]
}
